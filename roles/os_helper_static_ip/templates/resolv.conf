{% if os_helper_dns is defined -%}
{% if os_helper_dns is string -%}
nameserver {{ os_helper_dns }}
{% else %}
{% for nameserver in os_helper_dns -%}
nameserver {{ nameserver }}
{% endfor %}
{% endif %}
{% endif %}
{% if os_helper_domain is defined -%}
search {{ os_helper_domain }}
domain {{ os_helper_domain }}
{% endif %}
